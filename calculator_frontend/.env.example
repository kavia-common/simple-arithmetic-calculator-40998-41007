# React (CRA) environment variables must be prefixed with REACT_APP_ to be exposed to the browser bundle.
# This project does not require any env vars by default for the calculator UI.

# Optional examples (uncomment if your deployment/runtime needs them):
# REACT_APP_NODE_ENV=development
# REACT_APP_PORT=3000
